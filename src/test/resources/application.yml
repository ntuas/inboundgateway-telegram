management:
  endpoints:
    enabled-by-default: false
    web:
      base-path: /
  endpoint:
    info:
      enabled: true
    health:
      enabled: true

inboundgateway:
  telegram:
    bot:
      username: NtuasBot
      token: test
  backend:
    product:
      queue: products.manage.queue