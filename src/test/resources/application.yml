inboundgateway:
  telegram:
    bot:
      username: NtuasBot
      token: test

management:
  endpoints:
    enabled-by-default: false
  endpoint:
    info:
      enabled: true
    health:
      enabled: true