inboundgateway:
  telegram:
    bot:
      username: NtuasBot
      token: test

management:
  endpoints:
    enabled-by-default: false
    web:
      base-path: /
  endpoint:
    info:
      enabled: true
    health:
      enabled: true